<!-- View: Services -->

{% if data.items %}

	<div id='services-view' data-aos="fade-right">
	
    <!-- Banner Section -->
		<section class="hero-banner position-relative">
			{% if data.banner %}
				<div class="banner-background">
					<img src="{{ file_url(data.banner) }}" alt="{{ 'Banner'|t }}" class="w-100 h-100 object-fit-cover">
				</div>
			{% endif %}

			<!-- Overlay for better text readability -->
			<!-- <div class="banner-overlay position-absolute top-0 start-0 w-100 h-100"></div> -->
			<!-- {% if data.text %} -->
			<!-- Banner Content -->
			<!-- <div class="banner-content position-relative z-3">
				<div class="container">
					<div class="row justify-content-center align-items-center min-vh-75">
						<div class="col-lg-8 col-md-10 text-center text-white">
								<h1 class="display-4 fw-bold mb-4">{{ 'Services'|t }}</h1>
								<p class="lead mb-0">{{ data.text|slice(0, 200) }}
									{% if data.text|length > 200 %}...
									{% endif %}
								</p>
						</div>
					</div>
				</div>
			</div> -->

			{% if data.text %}
			<div class="pi-text">
				<p>{{ data.text|slice(0, 200) }}</p>
			</div>
			{% endif %}
      {% endif %}
		</section>

		<div class="separator"></div>
		{#  <h2 class="view-title"> {{ 'Services'|t }} </h2> #}
		
		<div class='services-items view-content container'>

			<div class="views-view-grid horizontal cols-3 clearfix">
				<div class="items">

					<!-- Services -->
					{% for item in data.items %}

						<div class="service">
							<div class="service-icon">
								<img class="svg-icon" src={{ file_url(item.get('field_image').entity.uri.value) }} alt="service name">
							</div>
							<h3> {{ item.get('title').value }} </h3>
							<p class="summary"> {{ item.get('field_description').value | raw }} </p>
						</div>

					{% endfor %}

				</div>
			</div>

		</div>
		
	</div>
  
{% endif %}	